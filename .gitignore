# build output
dist/
# generated types
.astro/
# build-time pulled data + downloaded audio (regenerated by scripts/pull.mjs on every build).
# writing.json + art/ ARE tracked: Substack 403s GitHub Actions IPs, so CI can never pull
# them — publish.cmd refreshes them from the owner's machine instead.
src/generated/films.json
src/generated/lastplayed.json
src/generated/music.json
src/generated/signals.json
public/audio-cache/
# deps
node_modules/
# env / secrets
.env
.env.*
!.env.example
# os / editor
.DS_Store
Thumbs.db
# scratch
tmp/
# local tooling + internal notes — not part of the published site
.claude/
/BUILDPLAN.md
/PLAN.md
/COUNCIL-REVIEW.md
/wireframe.html
